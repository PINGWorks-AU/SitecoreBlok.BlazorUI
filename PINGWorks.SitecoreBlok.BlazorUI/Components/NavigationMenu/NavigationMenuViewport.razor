@namespace PINGWorks.SitecoreBlok.BlazorUI

@* NavigationMenuViewport is rendered internally by NavigationMenu when Viewport="true" (the default).
   This component exists for API parity with Blok's exported NavigationMenuViewport.
   Consumers do not need to add this manually — NavigationMenu handles it. *@
