@page "/primitives/navigation-(side)"
@rendermode InteractiveServer

<ComponentPage Title="Navigation (Side)" Description="App-level side navigation — see Sidebar." Interactivity="ComponentInteractivity.Interactive">

	<DivergenceNote>
		<p><strong>Called <code>Sidebar</code> in BlazorUI.</strong> Blok lists this as "Navigation (Side)". The BlazorUI equivalent is <a href="/primitives/sidebar" class="text-primary underline"><code>Sidebar</code></a> — a collapsible app-level side navigation panel.</p>
		<p>Visit <a href="/primitives/sidebar" class="text-primary underline">/primitives/sidebar</a> for usage examples and the full API.</p>
	</DivergenceNote>

</ComponentPage>
